import { map } from '.jsx';
import { nullTag } from '.jsx';
import { seq } from '.jsx';
import { string } from '.jsx';
import { binary } from '.jsx';
import { trueTag, falseTag } from '.jsx';
import { floatNaN, floatExp, float } from '.jsx';
import { intBin, intOct, int, intHex } from '.jsx';
import { merge } from '.jsx';
import { omap } from '.jsx';
import { pairs } from '.jsx';
import { set } from '.jsx';
import { intTime, floatTime, timestamp } from '.jsx';

const schema = [
    map,
    seq,
    string,
    nullTag,
    trueTag,
    falseTag,
    intBin,
    intOct,
    int,
    intHex,
    floatNaN,
    floatExp,
    float,
    binary,
    merge,
    omap,
    pairs,
    set,
    intTime,
    floatTime,
    timestamp
];

export { schema };
